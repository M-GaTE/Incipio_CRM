{{ form_start(form) }}

    <table class="table table-bordered table-striped standard">
        <tr>
            <th>{{ form_label(form.nom) }}</th>
            <td>{{ form_widget(form.nom) }}</td>
        </tr>
        <tr>
            <th>{{ form_label(form.description) }}</th>
            <td>{{ form_widget(form.description) }}</td>
        </tr>
        <tr>
            <th>{{ form_label(form.membres) }}</th>
            <td>{{ form_widget(form.membres, {'attr': {'class': 'select2-multiple', 'style': 'width: 100%'}}) }}</td>
        </tr>
        <tr>
            <th>{{ form_label(form.etudes) }}</th>
            <td>{{ form_widget(form.etudes, {'attr': {'class': 'select2-multiple', 'style': 'width: 100%'}}) }}</td>
        </tr>
    </table>
    {{ form_widget(form) }}

    <input type="submit" value="Enregistrer la compétence" class="btn btn-success"/>

{{ form_end(form) }}

<script type="text/javascript">
    $(".select2-multiple").select2();
</script>
