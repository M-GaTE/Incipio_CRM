{# Formulaire d'ajout d'une étude #}

{{ form_start(form) }}

	{{ form_widget(form) }}

	<input type="submit" value="{{ 'suivi.avenant_mission_enregistrer'|trans({}, 'suivi') }}" class="btn btn-default"/>
{{ form_end(form) }}
