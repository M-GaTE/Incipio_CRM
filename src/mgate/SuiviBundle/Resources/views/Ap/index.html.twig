{% extends "mgateSuiviBundle::layout.html.twig" %}

{% block title %}
	{{ 'suivi.accueil'|trans({}, 'suivi') }} - {{ parent() }}
{% endblock %}

{% block content_bundle %}

    <h2>{{ 'suivi.ap_liste'|trans({}, 'suivi') }}</h2>

    <ul>

        {% for etude in etudes %}
            <li>
                <a href="{{ path('mgateSuivi_etude_voir', {'nom': etude.nom}) }}"> {{ 'suivi.etude.numero'|trans({}, 'suivi') }} {{ etude.id }}</a>
                , {{ 'suivi.etude.faite_par'|trans({}, 'suivi') }} {{ etude.mandat }} {{ 'suivi.etude.cree_le'|trans({}, 'suivi') }} {{ etude.dateCreation|date("d/m/Y") }}
            </li>
        {% else %}
            <li>{{ 'suivi.etude.pas_encore'|trans({}, 'suivi') }}</li>
        {% endfor %}

    </ul>

{% endblock %}
